{
	"name": "judge-search",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"test": "node --test src/*.test.ts",
		"check": "tsc --noEmit && biome check . && node --test src/*.test.ts"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.11",
		"@cloudflare/workers-types": "^5.20260903.1",
		"typescript": "^5.7.0",
		"wrangler": "^4.129.0"
	}
}
