# JudgeSearch SF watcher credentials
# Copy to ~/.config/judgesearch/credentials.env and fill in real values.
# (Owner-readable only: chmod 600 ~/.config/judgesearch/credentials.env)

# REQUIRED — bearer token for /api/upload (same value configured on the
# Cloudflare Worker via `wrangler secret put UPLOAD_SECRET`)
UPLOAD_SECRET=

# OPTIONAL — Telegram bot used to alert on scraper failures and big
# data-shift events. If unset, the watcher logs to
# ~/Library/Logs/judgesearch-sf-watcher.out.log only.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
